# Добавление функции get_version()

## Описание

Добавлена функция `get_version()`, которая возвращает текущую версию приложения — `"0.1.0"`.

## Файлы

- `src/app.py` — добавлена функция `get_version()` (строка 11)

## Категория

Разработка

## Влияние

Минимальное. Новая функция не затрагивает существующий код. Может использоваться другими модулями для получения версии приложения.
